version: "3.8"

services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - PORT=3000
    volumes:
      - sqlite-data:/app/data
    expose:
      - "3000"

volumes:
  sqlite-data:
